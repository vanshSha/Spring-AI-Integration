services:
  prometheus:
    image: prom/prometheus:latest
    container_name: prometheus
    ports:
      - "9090:9090"
    volumes:
      - ./application.yml:/etc/prometheus/application.yml
    networks:
      - monitoring

networks:
  monitoring:
    driver: bridge